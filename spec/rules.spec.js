/*jslint */
/*global describe, it, expect */

(function (describe, it, expect) {
    "use strict";

    describe("Testing", function () {
        it("for a first time", function () {
            expect("hello world").toEqual("hello world");
        });
    });
}(describe, it, expect));
